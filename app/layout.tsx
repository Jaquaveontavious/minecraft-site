import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Providers } from "./providers";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Truly Survival — Community Vanilla Minecraft",
  description:
    "Truly Survival is a community-driven vanilla Minecraft SMP with quality-of-life additions. Join now and experience balanced survival gameplay.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable}`}
    >
      <body className="min-h-screen bg-[#080d08] text-[#e8f5e9] antialiased">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
